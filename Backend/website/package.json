{
  "name": "backend",
  "version": "1.0.0",
  "description": "```bash # สร้าง virtual environment (ถ้ายังไม่มี) python3 -m venv venv source venv/bin/activate",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prisma": "node_modules/.bin/prisma",
    "db:push": "node_modules/.bin/prisma db push",
    "db:migrate": "node_modules/.bin/prisma migrate dev",
    "db:generate": "node_modules/.bin/prisma generate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prisma": "^6.19.2"
  }
}
